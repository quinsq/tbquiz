class Settings:
    """a class to store all settings for the quiz."""

    def __init__(self):
        """initialise the game's static settings."""
        #screen settings
        self.screen_width = 750
        self.screen_height = 600
        self.bg_color = (30, 30, 30)
